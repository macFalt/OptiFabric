@model OptiFabricMVC.Application.ViewModels.ProductsVM.ProductDetailsVM

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<h2>title</h2>

<div class="table-responsive">
    <table class="table table-bordered table-striped">
        <thead class="thead-dark">
        <tr>
            <th>Nazwa</th>
            <th>Numer rysunku</th>
            <th>Materiał</th>
        </tr>
        </thead>
        <tbody>
        <tr>
            <td>@Html.DisplayFor(modelNv => modelNv.Name)</td>
            <td>@Html.DisplayFor(modelNv => modelNv.DrawingNumber)</td>
            <td>@Html.DisplayFor(modelNv => modelNv.Material)</td>
        </tr>
        </tbody>
    </table>
</div>

